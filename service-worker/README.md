# Service Worker

Service Worker module for skillmap - handles skill tree data persistence using OPFS (Origin Private File System) and manages communication between the main app and the service worker.

## Development

```bash
npm install
npm run build       # compiles TypeScript to public/
npm run test        # runs unit tests
```
